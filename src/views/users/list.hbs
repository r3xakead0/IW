<div class="container-fluid p-2">
    <div class="row">
        <div class="col col-xs-4">
            <a href="/users/add" class="btn btn-success">
                <i class="fas fa-plus" title="Nuevo"></i>
                <span>Nuevo</span>
            </a>
        </div>
        <div class="col col-xs-8">
            <nav aria-label="navigation" class="float-right" role="pagination">
                {{{paginate pagination }}}
            </nav>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <span class="d-block bg-primary text-white text-center">Listado de Usuarios</span>
        </div>
    </div>
    <table class="table table-striped table-sm">
        <thead>
            <tr class="text-center">
                <th scope="col">Usuario</th>
                <th scope="col">Nombre Completo</th>
                <th scope="col">Email</th>
                <th scope="col">Perfil </th>
                <th scope="col">Estado</th>
                <th scope="col">Accion</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <td scope="row">{{name}}</td>
                <td>{{fullname}}</td>
                <td>{{email}}</td>
                <td>{{profile.name}}</td>
                <td class="text-center">
                    {{#if active}}
                    Activo
                    {{else}}
                    Inactivo
                    {{/if}}
                </td>
                <td class="text-center">
                    <a href="/users/edit/{{id}}" >
                        <i class="fas fa-marker" title="Modificar"></i>
                    </a>
                    <a href="/users/{{id}}?_method=DELETE">
                        <i class="far fa-trash-alt" title="Eliminar"></i>
                    </a>
                </td>
            </tr>
            {{else}}
            <tr class="text-center">
                <td colspan="6">No existen registros</td>
            </tr>
            {{/each}}

        </tbody>
    </table>
</div>
</div>